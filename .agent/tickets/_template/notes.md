# TICKET-ID — Notes

> State lives in `ticket.json`. Do not restate status here and do not use `- [ ]` checkboxes —
> this file has no authority over progress, and the board script warns if it tries to claim any.

## Decisions
*Why we chose this approach. Promoted into `.agent/memory/domains/*.md` when the ticket completes.*

## Gotchas
*Non-obvious traps discovered while implementing. Also promoted to memory on completion.*

## Open Questions
*Unresolved with the user or the codebase. Delete each line once answered.*

## Blockers
*Long-form detail only — the one-line summary and the open/closed flag live in `ticket.json`.
Use a `### blk-N` heading per blocker so `notesAnchor` can point at it.*

## Session Log
*Append-only, newest last. One `### YYYY-MM-DD` heading per working session.*
